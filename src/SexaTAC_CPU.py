#SexaTAC_CPU carries out the instructions of a program by passing the
#basic arithmetic, logical, control and input/output (I/O) operations to handlers

import TAChMemory

#int fetch(int addr)
#void store(int addr, int contents)

memory = TAChMemory()
pc = 0 #program counter
instuction = 0 #instuction pointer
opc = 0 #
flags
address
run_status = 0;

def execute():
def parse():
    opc = instruction 28 & 0x0000000f;
